datasource:
  driverClassName: oracle.jdbc.OracleDriver
  url: jdbc:oracle:thin:@yb-oradev:1521:orcl
  username: dev_nl_brickparking
  password: dev_nl_brickparking

activation:
  delay: 5000

mutator: BATCH v. ${project.version}

---

spring:
  profiles: test
  # for tests we use the built-in spring datasource
  datasource:
    driverClassName: org.hsqldb.jdbcDriver
    url: jdbc:hsqldb:mem:PUBLIC:syntax.ora=true

mutator: TEST MUTATOR
